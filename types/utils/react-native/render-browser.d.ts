/**
 *
 * @param {URL} src
 * @param {object} opts
 * @returns {[object, () => void]}
 */
export function renderBrowser(src: URL, opts?: object): [object, () => void];
import { URL } from "@onflow/fcl-core";
